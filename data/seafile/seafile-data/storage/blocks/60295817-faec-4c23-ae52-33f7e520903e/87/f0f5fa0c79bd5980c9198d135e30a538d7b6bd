variable "role_templates_info" {
  description = "Map of rule templates"
  type = map(object({
    context = string
    rules = list(object({
      api_groups = list(string)
      resources  = list(string)
      verbs      = list(string)
    }))
  }))
}