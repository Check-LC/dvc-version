
# Postgres 监控字段设计
## pg_wal
- 当前 pg_wal 缓冲区使用大小(进度条)
- 最大、最小缓冲区大小(进度条)
- 历史 pg_wal 缓冲区使用情况 (折线图)

## checkpoint
- checkpoint 周期 （字符显示）
- 上一次的 checkpoint 时间 （字符时间显示，或者下一次 checkpoint 到来的时间）
- 实时 checkpoint 写入数据量（或者上次 checkpoint 写入的总数据量）
- checkpoint 后端写入时长

## 